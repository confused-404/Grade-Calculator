main.rs: route http requests to right endpoint


/gpa-effect

gpa_effect.rs:
    GET: 
        returns: effect on current GPA
        parameters:
            current GPA (decimal value),
            # of classes taken (integer value),
            new expected grade (A | B | C | D | F)

/score-needed

score_calcs.rs:
    GET:
        returns: percentage grade needed to get planned grade
        parameters:
            weights (optional, if none, all equally weighted, array),
            points (array, only one element needed if all equally weighted),
            possible points (array, only one element needed if all equally weighted),

            ^^^ 3 above need to be ordered the same

            intended grade in percent (decimal value)

